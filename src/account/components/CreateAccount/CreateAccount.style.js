// import { colors } from 'rbv-core/colors';
import * as signinStyles from '../Signin/Signin.style';

export const formContainer = {
  ...signinStyles.formContainer
};
export const formTextColor = signinStyles.formTextColor;
export const submitButton = signinStyles.submitButton;
export const textInput = signinStyles.textInput;
export const textInputContainer = signinStyles.textInputContainer;
export const inputStyle = signinStyles.inputStyle;
